# Copy to .env locally. Do not commit real keys.
# Official HeroUI Pro CI variable is HEROUI_AUTH_TOKEN.
# This project also accepts HEROUI_KEY; map them when installing:
#   export HEROUI_AUTH_TOKEN="$HEROUI_KEY"
HEROUI_KEY=
HEROUI_AUTH_TOKEN=
